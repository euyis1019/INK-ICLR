\section{效果与适用范围}
\label{sec:experiments}

\paragraph{主实验。}
表~\ref{tab:main-summary} 摘录已有正式结果。CLIP ViT-B/32 八任务上，四个校准 seed 的均值为 $87.06\%$。T5-base 的 $85.64$ 包含由无标签留出 KL 选择的目标插值扩展（$\alpha=0.3$）；T5-large 选择 $\alpha=0$，即原版方法。因此，两种规模共同支持迭代框架的可用性，但 base 的成绩不单独归因于原版统计与求解。

\begin{table}[htbp]
\centering\small
\caption{主实验摘要。分数沿用各基准口径，不跨行相减。T5 的 $\pm$ 沿用原记录的跨 seed 汇总；移植 FeatCal 的 base 使用 5 seed，本方法使用 3 seed。}
\label{tab:main-summary}
\input{motivation_method/tables/main_summary}
\end{table}

\paragraph{跨任务规模与模型设定。}
在独立的 CLIP 工作台实现中，三个骨干与 $8/14/20$ 任务构成的九格均较 Iso-C 起点提高 $1.38$--$16.66$ 个百分点。该实现的 ViT-B/32 八任务结果为 $86.60\%$，不与正式实现的 $87.06\%$ 混为同一次运行。T5 使用教师答案上的 teacher-forced 分布，LLM 使用教师 top-128 logits；两者保留“当前统计、逐层求解、留出选步、接受后重测”的共同流程，具体教师近似和搜索粒度按设定适配。

\begin{table}[htbp]
\centering\small
\caption{LLM 的起点与后处理结果（单 seed）。同一行比较同一模型及指标；这是起点前后对照，不是所有方法的排名表。}
\label{tab:llm-summary}
\input{motivation_method/tables/llm_summary}
\end{table}

表~\ref{tab:llm-summary} 同时保留收益与下降。Llama 的 RegMean 起点在 GSM8K 上下降，TSV-M 起点的 IFEval 也下降；完整记录中 Llama 的三域宏平均最高仍是 TSV-M 起点自身。Gemma 的 Localize-and-Stitch 为 $51.33$，高于所列后处理结果。因此，我们主张的是\emph{同一迭代框架在多种设定中可用}，而不是所有任务均提升、所有基线均被超越，或完全不需设定适配。

\paragraph{起点与组件性质。}
初始化适配作为附加性质检验：ViT-B/32 的 Average 与 RegMean 诊断准确率差距由 $22.36$ 缩到 $0.15$ 个百分点（图~\ref{fig:motivation}a），但未拉平对照仍保留明显差距（附录~\ref{app:protocol}）。归一化的同机直接消融为 $87.0451\%$ 对 $86.6652\%$，其几何解释及优势条件见附录~\ref{app:normalization}。这些结果分别检验起点适配与组件作用，并与图~\ref{fig:motivation}b/c 的迭代和度量诊断互补。
